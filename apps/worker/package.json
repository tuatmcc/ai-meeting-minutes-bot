{
	"name": "@ai-meeting-minutes/worker",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"test": "vitest",
		"cf-typegen": "wrangler types",
		"register:commands": "node scripts/register-commands.mjs"
	},
	"dependencies": {
		"aws4fetch": "^1.0.20"
	},
	"devDependencies": {
		"@cloudflare/vitest-plugin": "^1.0.0",
		"typescript": "^5.5.2",
		"vitest": "~4.1.0",
		"wrangler": "^4"
	}
}
