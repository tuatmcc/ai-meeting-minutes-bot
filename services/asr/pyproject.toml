[project]
name = "meeting-asr"
version = "0.1.0"
description = "GPU-side Discord voice capture and speech recognition service"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[project.scripts]
meeting-asr = "meeting_asr:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = ["ruff>=0.16.9"]
